<section class="section pt-5">
  <div class="container">
    <div class="columns">
      <% if user_signed_in? %>
        <%= render 'profile' %>
      <% else %>
        <%= render 'trends' %>
      <% end %>
      <%= render 'feed', tweets: @tweets %>
      <%= render 'who-to-follow' %>
    </div>
  </div>
</section>
